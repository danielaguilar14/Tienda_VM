spring.application.name=Tienda

#Se esatblece el puerto por donde escucha el servidor
server.port=80

#Se elimina el banner de Spring
spring.main.banner-mode=off

#Se establece el formato de fecha/hora del log
logging.pattern.dateformat=hh:mm

#Se evita el uso de cache en el sistema en desarollo (false)
#En produccion si es necesario (true)
spring.thymeleaf.cache=false